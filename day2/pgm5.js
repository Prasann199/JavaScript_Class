const course={
    coursename:"JavaScript",
    instructor:"Prasann",
}
console.log(course.coursename);// we can access the data from object like this also and we can also access as shown below

const {coursename}=course//in this piece of code the coursename from the coourse will be stored in coursename
console.log(coursename);

